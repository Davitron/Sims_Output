const squareAllNumbers = (number) => {
    // if (number !== parseInt(number)) return "method parameter must be an integer";
    // const outputStr = number.toString().split('').map((digit) => {
    //     return digit * digit;
    // }).join('');
    // return parseInt(outputStr);
}

export default squareAllNumbers;